apiVersion: apps/v1
kind: Deployment
metadata:
 name: backend-deployment
 labels:
   type: was
   app: backend
spec:
 selector:
   matchLabels:
    type: was
    app: backend
 replicas: 2
 template:
   metadata:
     labels:
       type: was
       app: backend
   spec:
    containers:
    - name: container
      image: lllth94/backend:latest
      envFrom:
        - configMapRef:
            name: mysql-cm
        - secretRef:
            name: mysql-sec
      ports:
      - containerPort: 8000
      resources:
        requests:
          cpu: 100m
        limits:
          cpu: 200m